# HTTP — redirect to HTTPS (enable after certbot; disable bootstrap conf).
server {
  listen 80;
  server_name quanta.devclub.in;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}
